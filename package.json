{
  "name": "part3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui":"rm -rf dist && cd ../part2/phonebook && npm run build && cp -r dist ../../part3",
    "deploy:full":"npm run build:ui && git add . && git commit -m 'new build' && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ayaj Husain",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "morgan": "^1.10.0"
  }
}
